{% trans_default_domain 'layout' %}

{% trans_default_domain 'dashboard' %}
{% trans_default_domain 'document' %}
{%  set locale = app.request.getLocale() %}
{% if locale =="ar_MA"%} 
{% set text =  "now"|date("Y") ~' © '~'جميع الحقوق محفوظة' %} 
{% set var1 ='cClass' %}
{% else %}
{% set text ='Tous droits réservés  © ' ~"now"|date("Y") %}
{% set var1 ='' %}
{% endif %} 

<style>
        .cClass{
            direction: ltr;
            display: flex;
            flex-direction: row-reverse;
        }

</style>

<footer class="footer">
    <div class="container">
        <div class="row align-items-center flex-row-reverse">
            <div class="col-auto ml-lg-auto">
                <div class="row align-items-center">
                    <div class="col-auto">
                        <ul class="list-inline list-inline-dots mb-0">
                            <li class="list-inline-item"><a href={{ path('doc_index') }}>{{ 'documentation' | trans}}</a></li>
                        </ul>
                    </div>
                </div>
            </div>
            <div class="col-12 col-lg-auto mt-3 mt-lg-0 text-center {{var1}}">
                <span>{{text}} </span><a style="margin:0 10px 0 10px" href="http://aklitec.com">Aklitec.Com</a>
            </div>
        </div>
    </div>
</footer>